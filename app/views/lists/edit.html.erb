<1>編集画面</1>
<%= form_with model: @list, url: update_list_path(@list.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <H4>本文</H4>
  <%= f.text_area :body %>
  
  <%= f.submit '保存' %>

<% end %>